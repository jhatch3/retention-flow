# Runtime dependencies for the RetentionFlow backend package.
#
# Mirrors [project.dependencies] in pyproject.toml. `pip install -e .` is the
# canonical install; this file supports plain `pip install -r requirements.txt`.
# Dev tooling (pytest) installs via `pip install -e ".[dev]"`.
# The Dagster orchestration package has its own deps (src/orchestration/pyproject.toml).

# Database & migrations
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9

# Config & validation
pydantic>=2.0
pydantic-settings>=2.0

# Data & classical ML
pandas>=2.0
xgboost>=2.0
scikit-learn>=1.3
matplotlib>=3.8

# MLOps
mlflow>=2.16

# LLM — retention-email generator + LLM-as-judge
anthropic>=0.40
python-dotenv>=1.0

# API
fastapi>=0.110
uvicorn>=0.30
